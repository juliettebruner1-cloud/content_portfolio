import { Testimonial } from "@/types";

/**
 * EDIT ME — real testimonials only. Ships empty; the Testimonials
 * section does not render at all until at least one is added.
 *
 * { id: "t1", quote: "...", author: "Name", title: "Title", company: "Company" }
 */
export const testimonials: Testimonial[] = [];

import { CurrentlyItem } from "@/types";

/**
 * EDIT ME — the small "Currently" section that keeps the site feeling
 * alive. Leave the array empty to hide the section entirely; only add
 * lines that are actually true right now.
 */
export const currently: CurrentlyItem[] = [
  { label: "In", value: "New York City" },
];
